/**
 * Sentinel value representing a question/item with no response yet.
 */
export const MISSING_VALUE = '-99';

/**
 * Sentinel value representing a question/item that was hidden/filtered
 * out by conditional logic (and therefore has no meaningful response).
 */
export const MISSING_FILTERED_VALUE = '-77';
